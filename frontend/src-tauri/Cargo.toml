[package]
name = "marketlens"
version = "0.1.0"
description = "MarketLens desktop shell (Windows-first)"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"

[profile.release]
strip = true
