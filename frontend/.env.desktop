# Used by `vite build --mode desktop`. The desktop shell injects the real backend address (free port chosen
# at launch) as window.__MARKETLENS_API__, which takes precedence; this value is only a fallback.
VITE_API_BASE=http://127.0.0.1:8765
